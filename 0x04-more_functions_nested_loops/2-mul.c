#include "main.h"
/**
 * mul - muliplies
 * @a: an integer
 * @b: an integer
 * Return: a * b always
 * description: a program that multiplies two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
